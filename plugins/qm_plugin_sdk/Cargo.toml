[package]
name = "qm_plugin_sdk"
version = "0.1.0"
edition = "2021"
description = "Write qm-rs WasmEdge plugin modules as one safe Rust function."
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
